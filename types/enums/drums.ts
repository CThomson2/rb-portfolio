// Enum for drum status
export enum DRUM_STATUS {
  AVAILABLE = "available",
  SCHEDULED = "scheduled",
  PROCESSED = "processed",
}

// Enum for locations
export enum DRUM_LOCATION {
  NEW_SITE = "new-site",
  OLD_SITE = "old-site",
}
